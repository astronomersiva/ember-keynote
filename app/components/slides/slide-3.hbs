<div ...attributes>
  {{#let (get-code-snippet "slide.hbs" unindent=false) as |snippet|}}
    <CodeBlock @code={{snippet.source}} />
  {{/let}}
</div>

{{#if @speaker}}
  <SpeakerControls @notes={{this.notes}} />
{{/if}}
