{{#let (concat "slides/slide-" this.slide) as |Slide|}}
  <Slide
    class="{{concat "slide-" this.slide}} {{if this.speaker "hidden md:flex" "flex"}}"
    @speaker={{this.speaker}} />
{{/let}}

{{#unless this.speaker}}
  <ToggleFullScreen />
{{/unless}}

{{outlet}}
